const color = ['white', 'red', 'black', 'yellow'];

// 배열의 길이 체크 (length)
document.write(color.length);
document.write('<br />');

// 배열의 길이 set
color.length = 2;
document.write(color.length);
document.write('<br />');
document.write(color);
